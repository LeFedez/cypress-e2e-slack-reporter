# action.yml
# Este archivo define tu GitHub Action.
# Asegúrate de que la primera línea 'name:' comience en la columna 1 (sin espacios al inicio).

name: 'Cypress E2E Regression Tests with Slack Notification'
description: 'Ejecuta pruebas Cypress E2E, genera reportes Mochawesome y notifica los resultados detallados a Slack.'
author: 'Tu Nombre / Tu Organización' # <-- IMPORTANTE: Cambia esto a tu nombre de usuario de GitHub o nombre de tu organización.

# La sección 'branding' define cómo se ve tu acción en el GitHub Marketplace.
# Puedes elegir iconos de Octicons y colores específicos.
# Más iconos de Octicons aquí: https://octicons.github.com/
branding:
  icon: 'bell'      # Icono recomendado para notificaciones (ej: 'bell', 'paper-airplane', 'megaphone')
  color: 'blue'     # Color recomendado para notificaciones (ej: 'blue', 'green', 'red', 'yellow', 'purple', 'orange', 'gray', 'white')

# La sección 'inputs' define los parámetros que los usuarios pueden proporcionar al usar tu acción.
# Cada input tiene una descripción, un estado 'required' (requerido), y un valor 'default' (por defecto) opcional.
inputs:
  node-version:
    description: 'La versión de Node.js a usar para ejecutar las pruebas Cypress. Ej: "18" o "20".'
    required: false
    default: '18'

  cypress-command:
    description: 'El comando completo para ejecutar Cypress. Útil para personalizar opciones como grabar o usar un navegador específico.'
    required: false
    default: 'npx cypress run --reporter mochawesome --reporter-options reportDir=cypress/results,overwrite=false,html=true,json=true'

  slack-webhook:
    description: '¡Muy importante! El secreto para tu URL del Webhook de Slack. Debes guardarlo en los Secretos de GitHub del repositorio que use esta acción.'
    required: true

  slack-username:
    description: 'El nombre del bot que aparecerá en la notificación de Slack.'
    required: false
    default: 'CyBot'

  slack-icon:
    description: 'La URL de una imagen para el icono del bot en Slack. Esta imagen aparecerá en la notificación de Slack.'
    required: false
    default: 'https://pbs.twimg.com/profile_images/1512090708181725184/KAPAXmDg_400x400.jpg'

  slack-title:
    description: 'El título principal del mensaje de Slack. Aparecerá en grande en la notificación.'
    required: false
    default: '*Cypress E2E Test Results*' # En negrita

  continue-on-cypress-error:
    description: 'Si es "true", la acción seguirá ejecutándose (y enviará la notificación a Slack) incluso si las pruebas de Cypress fallan. Si es "false", la acción se detendrá si fallan.'
    required: false
    default: 'true'

# La sección 'runs' define cómo se ejecuta tu acción.
# 'composite' significa que tu acción consiste en múltiples comandos de shell y otras acciones.
runs:
  using: "composite" # Indica que es una acción compuesta, ejecutando una secuencia de pasos.
  steps:
    # Paso 1: Descarga el código del repositorio. Esto es esencial para que la acción
    # acceda a tus archivos de prueba de Cypress y package.json.
    - name: Checkout Code
      uses: actions/checkout@v3

    # Paso 2: Configura el entorno de Node.js con la versión especificada.
    - name: Setup Node.js Environment
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }} # Usa el input 'node-version' proporcionado por el usuario.

    # Paso 3: Instala las dependencias del proyecto (Cypress, Mochawesome, etc.).
    # Este paso asume que un 'package.json' y 'package-lock.json' existen.
    - name: Install Project Dependencies
      shell: bash # Ejecuta el comando usando bash shell
      run: npm ci # 'npm ci' requiere un package-lock.json para instalaciones consistentes.

    # Paso 4: Ejecuta las pruebas de Cypress usando el comando proporcionado.
    # 'continue-on-error' asegura que el flujo de trabajo continúe incluso si las pruebas de Cypress fallan,
    # para que la notificación de Slack pueda ser enviada.
    - name: Run Cypress Tests with Mochawesome
      shell: bash
      run: ${{ inputs.cypress-command }} # Usa el input 'cypress-command'.
      continue-on-error: ${{ fromJSON(inputs.continue-on-cypress-error) }} # Convierte el input de cadena a booleano.

    # Paso 5: Extrae el resumen de las pruebas (pasadas, fallidas, saltadas) del reporte de Mochawesome.
    # Este paso se ejecuta 'always()' para asegurar que la notificación de Slack pueda enviarse incluso si tests fallan o el paso anterior tuvo un error.
    - name: Extract Test Summary
      if: always() # Asegura que este paso se ejecute sin importar el resultado del paso anterior.
      shell: bash
      run: |
        # Instala 'jq' (procesador de JSON) ya que podría no estar preinstalado en todos los runners.
        sudo apt-get update && sudo apt-get install -y jq

        RESULTS_FILE="cypress/results/mochawesome.json"

        # Verifica si el archivo de reporte de Mochawesome existe. Si no, las cuentas por defecto serán 0 y el color será rojo.
        if [ ! -f "$RESULTS_FILE" ]; then
          echo "WARNING: Mochawesome results file '$RESULTS_FILE' not found. Defaulting test counts to 0."
          echo "PASSED_TESTS=0" >> $GITHUB_ENV
          echo "FAILED_TESTS=0" >> $GITHUB_ENV
          echo "SKIPPED_TESTS=0" >> $GITHUB_ENV
          echo "PENDING_TESTS=0" >> $GITHUB_ENV 
          echo "TOTAL_TESTS=0" >> $GITHUB_ENV
          echo "SLACK_DYNAMIC_COLOR=danger" >> $GITHUB_ENV # Establece el color a rojo si no se encuentran resultados
        else
          # Parsea el reporte JSON de Mochawesome para obtener las estadísticas de las pruebas.
          echo "PASSED_TESTS=$(jq '.stats.passes' "$RESULTS_FILE")" >> $GITHUB_ENV
          echo "FAILED_TESTS=$(jq '.stats.failures' "$RESULTS_FILE")" >> $GITHUB_ENV
          echo "SKIPPED_TESTS=$(jq '.stats.skipped' "$RESULTS_FILE")" >> $GITHUB_ENV
          echo "PENDING_TESTS=$(jq '.stats.pending' "$RESULTS_FILE")" >> $GITHUB_ENV
          echo "TOTAL_TESTS=$(jq '.stats.tests' "$RESULTS_FILE")" >> "$GITHUB_ENV"

          # Determina el color de la notificación de Slack basándose en los fallos de las pruebas.
          if [ "$(jq '.stats.failures' "$RESULTS_FILE")" -gt 0 ]; then
            echo "SLACK_DYNAMIC_COLOR=danger" >> $GITHUB_ENV # Rojo para fallos
          else
            echo "SLACK_DYNAMIC_COLOR=good" >> $GITHUB_ENV  # Verde para éxito
          fi
        fi

    # Paso 6: Envía el resumen de los resultados de las pruebas a Slack.
    # Este paso también se ejecuta 'always()' para asegurar que la notificación se envíe sin importar los resultados de las pruebas.
    - name: Send Slack Notification
      if: always() # Asegura que este paso se ejecute sin importar el resultado del paso anterior.
      uses: rtCamp/action-slack-notify@v2 # Usa una GitHub Action preconstruida para notificaciones de Slack.
      env: # Variables de entorno pasadas a la acción de notificación de Slack.
        SLACK_COLOR: ${{ env.SLACK_DYNAMIC_COLOR }} # Usa el color determinado dinámicamente (verde/rojo).
        SLACK_USERNAME: ${{ inputs.slack-username }} # Usa el input 'slack-username'.
        SLACK_ICON: ${{ inputs.slack-icon }}         # Usa el input 'slack-icon'.
        SLACK_MESSAGE: | # El contenido principal del mensaje de Slack, formateado usando Markdown.

          Commit: `<https://github.com/${{ github.repository }}/commit/${{ github.sha }}>`
          Rama: `${{ github.ref_name }}`

          📊 *Resumen de Tests:*
          Total: *${{ env.TOTAL_TESTS }}*
          ✅ Pasadas: *${{ env.PASSED_TESTS }}*
          ❌ Fallidas: *${{ env.FAILED_TESTS }}*
          ⚪️ Saltadas: *${{ env.SKIPPED_TESTS }}*
          ⏳ Pendientes: *${{ env.PENDING_TESTS }}*

          🔗 Ver workflow: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        SLACK_TITLE: ${{ inputs.slack-title }}
        SLACK_WEBHOOK: ${{ inputs.slack-webhook }}
        SLACK_FOOTER: ''        # Elimina el pie de página "Powered By..."
        SLACK_MSG_AUTHOR: ''    # Usa SLACK_MSG_AUTHOR para el nombre del autor
        MSG_MINIMAL: true       # Elimina Ref, Event, Actions URL y Commit
        SLACK_FILE_UPLOAD: 'cypress/results/mochawesome.html' # Ruta a tu reporte HTML
        SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }} # Asegúrate de que este secreto esté configurado en GitHub